export enum AUTH {
  Login = '/login',
  Registration = '/registration',
  ResetPassword = '/reset-password',
}

export enum PRIVATE {
  About = '/about',
  Account = '/account',
  CreateEvent = '/events/create',
  Error = '/error',
  Events = '/events',
  NotFound = '/not-found',
  Settings = '/settings',
}

export enum PARAM {
  EventId = 'eventId',
}
