export enum AUTH {
  Login = '/login',
  Registration = '/registration',
  ResetPassword = '/reset-password',
}

export enum PRIVATE {
  Events = '/events',
  Account = '/account',
  Settings = '/settings',
}
