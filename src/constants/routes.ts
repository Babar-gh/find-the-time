export enum AUTH {
  Login = '/login',
  Registration = '/registration',
  ResetPassword = '/reset-password',
}

export enum PRIVATE {
  About = '/about',
  Account = '/account',
  CreateEvent = '/events/create',
  Events = '/events',
  Settings = '/settings',
}

export enum PARAM {
  EventId = 'eventId',
}
