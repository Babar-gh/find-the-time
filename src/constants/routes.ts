export enum AUTH {
  Login = '/login',
  Registration = '/registration',
  ResetPassword = '/reset-password',
}

export enum PRIVATE {
  About = '/about',
  Account = '/account',
  Events = '/events',
  Settings = '/settings',
}
